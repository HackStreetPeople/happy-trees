<nav class="navbar">

    <div class="welcome">
      <h1 class="has-text-black is-size-2 has-text-weight-bold">Happy Trees:</h1>
      <h2 class="has-text-black is-size-4 has-text-weight-bold">Making data easier to view since 2022!</h2>
      <h3 class="has-text-black is-size-6 has-text-weight-bold">An app for uploading and viewing tree planting data for mitigation companies</h3>
    </div>

    <div class="login has-text-black is-size-6 has-text-weight-bold">
      {{#if isloggedin}}
      You are logged in as {{user.firstName}}
      <a href="/logout"> <button type="btn" class="btn button is-black has-text-white is-size-6">Logout</button></a>
      {{else}}
      {{>modal/login-block}}
      {{/if}}
      <h5 id="login-err-msg"></h5>
    </div>

</nav>

{{!-- {{>modal/login-block}} --}}